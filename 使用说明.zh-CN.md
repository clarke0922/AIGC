# 叙幕短片工作台

基于 LocalMiniDrama 的 Windows 个人短片工具。上游版本：`7b6c1a7`，保留原项目 MIT 版权声明，原版工作台仍可从右上角进入。

## 启动

双击 `启动叙幕.cmd`，或运行 `desktop/release/win-unpacked/叙幕短片工作台.exe`。可将整个 `win-unpacked` 文件夹复制到其他 Windows x64 电脑运行，不要只复制 EXE。无需另装 Node.js 或 FFmpeg。

项目、密钥、素材默认保存在 `%APPDATA%/xumu-studio/backend`。密钥使用上游本地数据库存储，未实现加密保险箱；不要把该目录上传到公开仓库。完整备份请退出应用后复制该目录。下载工程 ZIP 包含内容、素材和费用记录，用于交付剪辑；目前不支持将该 ZIP 一键导回工作台。

## 第一次制作

1. 打开「雨停之前」示例，熟悉两名人物、一个场景、六个5秒镜头；也可以新建项目填写故事。
2. 打开「模型与费用设置」，添加自己的 DeepSeek 文本、Qwen 云转写、火山图片和支持原生对白的视频模型连接。填写供应商已开通的精确模型 ID，不要直接照抄网上的过期名称。图片协议使用火山同步单图；视频使用火山任务接口。
3. 输入每百万输入/输出 Token、每张图片、每秒转写/视频的人民币费用上界。图片目前请求2560×1440，视频默认720p；价格必须匹配所选规格及音频选项。保存连接后还要保存项目模型与费用。价格未知时不允许提交。
4. 在「故事」录音或上传音频，先预览转写再采用，也可以直接输入文字。录音最长约5分钟、上传最大10MB。生成的剧本也是候选，点击「使用此版本」才写入项目。
5. 在「角色与场景」生成或上传参考图。确定服装、声音描述及场景光线，再逐镜生成或上传静帧。图片任务也需要选择候选版本。
6. 确认剧本与全部参考图，再生成一个5秒对白试镜。观看人物、动作、对白和口型，按供应商账单对账，并勾选验收项。通过后才能生成其余镜头。视频模型或价格变化会重新要求试镜。
7. 在「成片」调整镜头顺序、剪辑入出点、音量和是否参与成片，合成后选择导出候选，下载 MP4 和 SRT。输出1280×720、24fps、H.264/AAC，字幕按镜头显示，并非逐字强制对齐。

## 修改与节省费用

- 每个对象支持直接编辑和一句话微调。微调先生成建议，再预览修改前后和影响范围；未采用的建议不覆盖作品。
- 修改某镜头只标记该镜头；改角色或场景会标记引用它的镜头。检查后可沿用视频，也可只重生成受影响镜头。人物声音以描述绑定，跨镜头音色一致性仍取决于供应商，不是独立声音克隆。
- 历史版本可恢复内容，但不会倒退账单。生成候选及旧素材保留在本机。上传自己的参考图无需云端生图费用。
- 项目默认最高50元。每次提交先预留费用，已用加预留不足时拦截。真实账单若高于填写的费用上界仍可能超过50元，因此必须先用试镜核对实际单价；工具不会自动追加预算。
- 超时且已有供应商编号时继续查询原任务。没有编号的未知任务不会自动重发，也不会自动退还预留；先到供应商控制台确认结果和费用，再对账。不要未经核实将实际费用填0。
- 本机导出不收模型费用。降低预算可缩短短片、减少镜头和动作、复用参考图，不建议盲目反复生成。

## 验收边界

已提供可运行软件、示例剧本和自动化测试。当前没有配置真实 API 密钥，没有生成真人样片，也没有发生真实供应商费用。`artifacts/media-test` 中的视频仅是色块和测试音，用来验证30秒合成、音轨和字幕，不能视为电影级样片。

真实麦克风、云端识别、参考图效果、跨镜头人物/音色一致性、口型与账单仍需用你的已开通模型完成5秒试镜后再验收。视觉目标是否达成以实际观看为准。

## 从源码构建

运行 `powershell -ExecutionPolicy Bypass -File scripts/setup-studio.ps1` 安装锁定依赖，再运行 `scripts/test-studio.ps1` 验证。首次安装需要网络。构建命令为在 `desktop` 执行 `npm.cmd run pack`。关闭已运行的交付版后再重新打包。

开发启动使用 `desktop` 下的 `npm.cmd start`，另需先构建并复制前端，并设置 `FFMPEG_PATH` 和 `FFPROBE_PATH` 到 `.runtime` 中的对应程序。开发与交付版默认使用同一用户数据目录；隔离测试可设置 `STUDIO_USER_DATA` 为单独目录。
