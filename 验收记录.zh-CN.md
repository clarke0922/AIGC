# 叙幕首版验收记录

日期：2026-09-05。平台：Windows x64；Electron 28.3.3；源码运行时 Node 22.16.0。上游固定为 LocalMiniDrama `7b6c1a7`。

提交前回归：后端73项、前端11项全部通过（含媒体测试）。新增项目软删除保护例外及火山模型目录读取/单候选选择测试。模型目录查询已实现固定方舟地址、禁止重定向、超时和手动填写回退；此次未取得真实模型目录，供应商端支持与账号权限尚待验证。

## 已验证

| 检查 | 结果 |
| --- | --- |
| 上游基线 | 后端60项、前端10项测试通过；原版服务与前端可构建运行 |
| 改造后后端 | 70项通过，0失败，0跳过 |
| 改造后前端 | 10项通过；Vite生产构建通过，保留上游大包体警告 |
| 预算、重复提交 | 预留事务、重复请求键、未知状态保留费用、价格缺失拦截通过 |
| 编辑与版本 | 第三镜编辑不覆盖兄弟镜头；角色/声音关联影响；过期版本拒绝；恢复不倒退账单通过 |
| 接口适配 | 本地模拟供应商验证文本、ASR、图片参考、视频任务编号和用量；断言视频提交仅一次并携带音频开关 |
| 实际合成 | FFmpeg生成30秒、六段视频，音轨及中文字幕存在；逐镜裁剪、拼接与最终探测通过 |
| Windows构建 | `npm.cmd run pack`成功，得到完整`win-unpacked`交付目录 |
| 打包应用启动 | `packaged=true`、后端5679就绪、窗口ready-to-show；FFmpeg及FFprobe自动提取成功 |
| 重启持久化 | 关闭并重启交付版后，示例名称、6镜头、版本及50元预算均保留 |
| 参考图上传 | 真实打包后端HTTP上传成功，版本递增，并仅标记引用该人物的4个镜头 |
| 来源限制 | 外部网页Origin请求模型接口返回403 |
| 交付内容隔离 | ASAR文件检查未包含backend-app/data或logs；原版权及媒体工具许可证随包提供 |
| 界面检查 | 同一打包后端的浏览器界面完成示例创建、分镜编辑预览、最终布局与对比度检查 |

## 尚未通过的真实制作验收

当前没有供应商密钥，未调用真实收费模型，实际费用为0元。模拟接口的Token用量和金额只是测试断言，不代表供应商报价或账单。

未完成真实麦克风录音及云识别验收、5秒真人对白试镜、30秒真人样片、人物服装与音色连续性、口型、肢体质量和50元内视觉目标验收。Windows原生窗口已启动且可读取控件，但电脑截图接口报错，未完成全套原生窗口鼠标操作验收；浏览器检查不等同于该项通过。

`artifacts/media-test/studio/exports/verified/film.mp4`为技术合成测试（色块与测试音），不可当作真人样片交付。示例工程仅有剧本、人物与分镜文本。

下一步是在应用中填写自己的模型密钥和精确价格，用一个5秒镜头核验真实供应商能力与账单，通过后继续其余镜头。若视觉不达标，记录不通过项并缩短或简化作品，不能据当前技术测试宣称达到电影级。

## 重现

执行 `powershell -ExecutionPolicy Bypass -File scripts/test-studio.ps1`。测试使用临时或内存数据库和本地模拟供应商，不触发收费生成。

交付版独立验收数据位于 `.runtime/packaged-acceptance`，开发版此前数据备份位于 `.runtime/desktop-dev-data-backup`；正常双击应用时使用 `%APPDATA%/xumu-studio`，不会带入这些验收数据。
